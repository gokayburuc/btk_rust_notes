pub fn vector_call() {
    // let mut a = Vec::new();

    // // deger ekleme
    // a.push(1);
    // a.push(2);
    // a.push(3);

    // println!("{:?}", a);
    // a.push(4);
    // println!("{:?}", a);

    let a: Vec<i32> = vec![1, 2, 3, 4]; // [1;10]
    println!("{:?}", a);

    //
    let index: usize = 1;
    println!("a[0] ={}", a[index]);
}
